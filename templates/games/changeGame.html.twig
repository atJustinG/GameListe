{% extends 'base.html.twig' %}

{% block title %}Games Liste{% endblock %}

{% block body %}

    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        <a href= "{{ path("app_game_index") }}"><button type="button" name="btnCancel">Abbruch</button></a>
    </div>
{% endblock %}